# Process 16_5_26 — Monthly Report (พฤษภาคม 2026)

> สรุปงานที่ทำระหว่างเดือน พ.ค. (อัปเดตล่าสุด: 5 มิ.ย. 2026)
> **121 tests, 164 assertions, 0 failures**

---

## 📋 Test Suite

| File | Tests | Coverage |
|---|---|---|
| BookingStateTest | 23 | Booking status state machine |
| RoomStateTest | 23 | Room status state machine |
| AuthTest | 9 | Register, login, logout, me |
| BookingTest | 17 | CRUD, lookup, search, validation, rate limiting |
| FrontDeskTest | 6 | Walk-in, check-in, check-out, payment, room type validation |
| PaymentTest | 6 | Payment request, webhook, guest email verification |
| RoomTest | 9 | Rooms/types, availability, status transitions |
| RouteProtectionTest | 19 | Public vs authenticated vs admin |
| UserTest | 6 | User CRUD, profile, role escalation prevention |

---

## ✅ Fixed Issues

### Security

| # | หัวข้อ | ไฟล์หลัก |
|---|---|---|
| #6 | Public booking routes ไม่มี rate limiting | routes/api.php |
| #15 | Register ยอมรับ role field → admin escalation | AuthController.php |
| #21 | `lookupBooking` OR logic → brute-force ง่าย → AND + บังคับ email | BookingController.php |
| #28 | `requestPaymentForGuest()` OR logic → AND + บังคับ email | PaymentController.php |
| #40 | Exception message รั่วใน error responses | 5 controllers |

### Mass Assignment

| # | หัวข้อ | ไฟล์หลัก |
|---|---|---|
| #8 | Booking `$guarded = []` → `$fillable` | Booking.php |
| #9 | StoreBookingRequest ยอมรับ server-only fields | StoreBookingRequest.php |
| #17 | Room + BookingRoom `$guarded = []` → `$fillable` + Room เพิ่ม HasUuids | Room.php, BookingRoom.php |
| #27 | Addon `$guarded = []` → `$fillable` | Addon.php |

### Payment & Receipt

| # | หัวข้อ | ไฟล์หลัก |
|---|---|---|
| #10 | Confirmation number `rand()` collision → atomic counter `booking_sequences` | Booking.php |
| #18 | `recordPayment` ไม่อัปเดต `is_paid` → แก้ + auto Receipt | FrontDeskController.php |
| #19 | Receipt number `rand()` → atomic counter `receipt_sequences` | Receipt.php |
| #29 | Webhook ไม่เช็ค `payment_deadline` → เพิ่ม deadline check | PaymentController.php |
| #30 | Payment/Receipt `amount` type mismatch → standardize เป็น `integer` (satang/cents) | Payment.php, Receipt.php, StorePaymentRequest.php, 2 migrations |
| #33 | `DB::raw('TRUE')` PostgreSQL-specific → `true` | PaymentController.php |

### Code Quality

| # | หัวข้อ | ไฟล์หลัก |
|---|---|---|
| #11 | Booking list ไม่มี pagination → `paginate(15)` | BookingController.php |
| #13 | DailyRoomMaintenance log แสดงสถานะผิด → เก็บ original status | DailyRoomMaintenance.php |
| #14 | Booking search inconsistent → merged เข้า `GET /bookings?term=` | BookingController.php, routes/api.php |
| #22 | DailyRoomMaintenance schedule → มีอยู่แล้วใน console.php | — |
| #23 | Expired draft cleanup → `CleanupExpiredDrafts` command + schedule | CleanupExpiredDrafts.php |
| #24 | LIKE wildcard abuse → escape `%` และ `_` | BookingController.php |
| #25 | Dead route `GET /bookings/addons` → ลบแล้ว | routes/api.php |
| #26 | Room state `occupied → prep_checkin` ขัด flow document → เพิ่ม transition | Room.php |
| #31 | Room availability query inconsistent → `whereIn` ตรงกับ booking logic | RoomController.php |
| #32 | `checkIn()` assign rooms ไม่ตรวจ room type → เพิ่ม validation | FrontDeskController.php |
| #34 | `$totalGuests` dead code → ลบแล้ว | BookingController.php |
| #35 | Walk-in `guest_email` ไม่ unique → `walkin-{phone}@hotel.local` | FrontDeskController.php |
| #41 | Room model redundant UUID config → ลบที่ซ้ำซ้อน | Room.php |

### Error Handling

| # | หัวข้อ | ไฟล์หลัก |
|---|---|---|
| #36 | `updateStatus()` error code ไม่ validate → แยก business logic จาก unexpected | BookingController.php |
| #37 | `createBooking()` error code ไม่ validate → แยก 422 จาก 500 | BookingController.php |
| #38 | Webhook exception handler `DB::rollBack()` → มีอยู่แล้ว | — |
| #39 | `createBooking()` invalid HTTP code → แยก business logic จาก server errors | BookingController.php |

### Bug Fixes (พบระหว่างพัฒนา)

| หัวข้อ | ไฟล์หลัก |
|---|---|
| BookingController response key `status` ซ้ำ → `booking_status` | BookingController.php |
| FrontDesk walk-in ไม่มี email → auto email from phone | FrontDeskController.php |
| FrontDesk walk-in ส่ง field ไม่มีใน schema (`room_price`, `subtotal`) → ลบออก | FrontDeskController.php |
| FrontDesk `recordPayment` undefined `reference_number` → `?? null` | FrontDeskController.php |
| UserController profile update → Role escalation → กรอง `role` ออก | UserController.php |
| StoreBookingRequest ยอมรับ server-only fields → ลบออก | StoreBookingRequest.php |
| `guest_nationality` ส่ง null ทับ default → migration nullable() | migration bookings |
| Walk-in booking ไม่มี explicit `status` → เพิ่ม `'status' => 'draft'` | BookingController.php, FrontDeskController.php |

---

## 📁 Files Changed

| ไฟล์ | Action |
|---|---|
| `app/Models/Booking.php` | `$guarded` → `$fillable` + `generateUniqueConfirmation()` + `system` role |
| `app/Models/Room.php` | `$guarded` → `$fillable` + `HasUuids` + เพิ่ม `occupied` transition + ลบ redundant config |
| `app/Models/BookingRoom.php` | `$guarded` → `$fillable` |
| `app/Models/Addon.php` | `$guarded` → `$fillable` |
| `app/Models/Receipt.php` | เพิ่ม `generateUniqueReceiptNo()` + cast `amount→integer` |
| `app/Http/Controllers/Api/V1/BookingController.php` | Confirmation + pagination + LIKE escape + explicit status + ซ่อน exception + ลบ dead code |
| `app/Http/Controllers/Api/V1/PaymentController.php` | Atomic receipt counter + AND logic + deadline check + `DB::raw` → `true` + ซ่อน exception |
| `app/Http/Controllers/Api/V1/FrontDeskController.php` | Confirmation + explicit status + `is_paid` + Receipt + email unique + room type validation + Log |
| `app/Http/Controllers/Api/V1/RoomController.php` | Availability query `whereIn` + Log |
| `app/Http/Controllers/Api/V1/DashboardController.php` | ซ่อน exception + Log |
| `app/Http/Controllers/Api/V1/AuthController.php` | Register pick safe fields only |
| `app/Http/Controllers/Api/V1/UserController.php` | กรอง `role` ออกจาก profile update |
| `app/Http/Requests/StoreBookingRequest.php` | ลบ server-only fields |
| `app/Console/Commands/DailyRoomMaintenance.php` | เก็บ original status ก่อน log |
| `app/Console/Commands/CleanupExpiredDrafts.php` | New — expired draft cleanup |
| `database/migrations/2026_06_04_043100_create_booking_sequences_table.php` | New |
| `database/migrations/2026_06_04_043200_add_unique_constraint_to_bookings_confirmation.php` | New |
| `database/migrations/2026_06_04_150000_create_receipt_sequences_table.php` | New |
| `database/migrations/2026_06_05_100000_change_payments_amount_to_integer.php` | New — decimal→bigint |
| `database/migrations/2026_06_05_100001_change_receipts_amount_to_integer.php` | New — decimal→bigint |
| `app/Models/Payment.php` | cast `decimal:2`→`integer` |
| `app/Http/Requests/StorePaymentRequest.php` | validation `numeric`→`integer` |
| `routes/api.php` | Rate limiting + ลบ dead route + route restructuring |
| `routes/console.php` | เพิ่ม schedule `CleanupExpiredDrafts` |

---

## 🔴 Remaining Issues

| # | หัวข้อ | ความรุนแรง | หมายเหตุ |
|---|---|---|---|
| #4 | Webhook ไม่มี HMAC signature verification | 🔴 Blocker | รอ Payment Gateway |
| #20 | Addon prices มาจาก client — trust client | 🟠 Major | Server ควร lookup ราคาเอง |

# ลบ field `payment_method` ออกจากทั้ง flow ใหม่ (booking_confirmations) และ legacy (payments) + ปลด freeze

**การตัดสินใจ (ยืนยันจากนายท่านแล้ว):** flow การจ่ายเงินตอนนี้คือ "ส่งสลิป → รอแอดมินตรวจ" เท่านั้น จึงเอา `payment_method` ออกจากทั้งสองฝั่ง พร้อมปลดสถานะ freeze ของตาราง `payments` · `transfer_time` เปลี่ยนเป็น optional · `slip_image` บังคับส่งเสมอ · `receipts` ยังคง frozen · webhook ยังคง 410

## 1. Migration ใหม่
สร้าง `database/migrations/2026_08_19_HHMMSS_drop_payment_method_columns.php`:
- `up()`: `dropColumn('payment_method')` ทั้ง `booking_confirmations` และ `payments`
- `down()`: คืน column เป็น `string nullable` (+ comment เดิม) ทั้งสองตาราง — ใช้ nullable เพราะคืนแบบ NOT NULL จะพังถ้ามี rows อยู่

## 2. Form Requests
- `app/Http/Requests/ConfirmBookingRequest.php` — rules ใหม่:
  ```php
  'slip_image'    => 'required|file|image|mimes:jpeg,png,jpg|max:4096',
  'transfer_time' => 'nullable|date|before_or_equal:now',
  ```
  (ลบ `payment_method` + เลิกใช้ `required_if`) และแก้ docblock ให้ตรง
- `app/Http/Requests/StorePaymentRequest.php:21` — ลบบรรทัด `payment_method`
- `app/Http/Requests/UpdatePaymentRequest.php:19` — ลบบรรทัด `payment_method`

## 3. Controllers
- `BookingConfirmationController::confirm` — ลบ `'payment_method' => $validated['payment_method']` ออกจาก `create()` · เก็บ slip ตรงๆ (`$request->file('slip_image')->store(...)`) เพราะบังคับแล้ว · `'transfer_time' => $validated['transfer_time'] ?? null` คงเดิม · `slip_image_url` ตอบ `Storage::url($slipPath)` ตรงๆ · แก้ comments ("slip + method + time" → "slip + time", ลบเรื่อง cash/credit_card)
- `PaymentController::requestPayment` (L48) — ลบ `'payment_method'` ออกจาก `Payment::create()`
- `FrontDeskController::recordPayment` (L468) — ลบ `'payment_method'` ออกจาก `Payment::create()`

## 4. Models
- `app/Models/BookingConfirmation.php:24` — ลบออกจาก `$fillable`
- `app/Models/Payment.php:16` — ลบออกจาก `$fillable`

## 5. Tests
- `tests/Feature/BookingConfirmationTest.php`:
  - `confirmPayload()` ลบ key `payment_method`
  - `test_owner_can_submit_confirmation_with_transfer_slip` → rename เป็น `test_owner_can_submit_confirmation_with_slip` + ลบ `payment_method` จาก `assertDatabaseHas`
  - `test_transfer_requires_slip_image` → `test_confirm_requires_slip_image` (ส่งไม่มี slip → 422)
  - `test_cash_payment_does_not_require_slip` → แทนที่ด้วย `test_transfer_time_is_optional` (ส่ง slip ไม่ส่ง transfer_time → 201)
  - ลบ `'payment_method' => ...` จาก `BookingConfirmation::create` fixtures ทั้ง 7 จุด
- `tests/Feature/PaymentTest.php` (L43, 60, 88, 96) และ `tests/Feature/FrontDeskTest.php` (L215, 251, 291, 320 + comment L317) — ลบ key ออกจาก payloads

## 6. Test scripts (manual)
- `test_scripts/api_test_chain.php:345`, `test_scripts/api_test_remote.php:424`, `test_scripts/api_guide.php:384` — ลบ `'payment_method' => ...` ออกจาก payload

## 7. Docs + memory
- `docs/api_guide.md` — L1244 (note), L1254 (body table), L1262 (curl) ของ confirm endpoint · L1723–1748 (front-desk payment) · L1762–1784 (POST /payments + webhook) · L2177–2184 (payments schema) · L2226 (booking_confirmations schema)
- `docs/booking-verify-flow.md` — L182, L190, L234
- `docs/database-er.md` — L135 (PAYMENTS), L161 (BOOKING_CONFIRMATIONS)
- `cline.md` — เพิ่ม changelog 19/08/26: ลบ payment_method ทั้งสอง flow + **ปลด freeze payments** + transfer_time optional; แก้ flow line L866 และ fillable note L296
- `AGENTS.md` — แก้ section "Frozen / Deprecated": `payments` ปลด freeze แล้ว (19/08/26, เหตุผล: เหลือ flow สลิปอย่างเดียว) · `receipts` ยัง frozen · webhook ยัง 410

## 8. Verification
1. `php artisan migrate` (local SQLite — drop column)
2. `php artisan test --filter=BookingConfirmationTest` → `PaymentTest` → `FrontDeskTest`
3. `php artisan test` (full suite)
4. `vendor/bin/pint --dirty`

**ผลข้างเคียงที่ยอมรับ:** response ของ admin (`verify`/`reject`/`pending`/`POST /payments`/`front-desk payment`) จะไม่มี field `payment_method` อีกต่อไป (breaking change ตั้งใจ — frontend `ku-home` ต้องเลิกอ่าน field นี้ด้วย)